import './insertNewComponent.modules.css'

export default function InsertNewComponent() {
    return (
        <div
            className='add-new-component'
            onClick={() => console.log('click')}
        ></div>
    )
}
